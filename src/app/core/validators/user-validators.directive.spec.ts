import { UserValidatorsDirective } from './user-validators.directive';

describe('UserValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
